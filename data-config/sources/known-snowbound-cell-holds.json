{
  "schemaVersion": 1,
  "destinationIds": [
    "zermatt",
    "el-chalten",
    "garhwal"
  ],
  "method": "Temporary explicit allowlist for already-published representative cells whose snow never melts. A reviewed replacement must remove its id in the same verified dataset commit."
}
