# Repository review, 7 September 2026

Reviewed current upstream `bb66ce4`, not the obsolete local 50-destination
checkout. The current catalogue contains 315 destinations and 3,780 months.

## Fixed

- **Navigation disappeared on tablets.** Desktop links were hidden at 980px,
  but the menu appeared only at 720px. Partial hiding also removed two links at
  1050px. The complete menu now replaces the desktop navigation at 1050px.
- **The empty shortlist was a dead end.** Removing its last destination hid the
  switch needed to leave shortlist mode. That switch now remains available,
  and Reset filters clears shortlist mode and resets pagination.
- **Shared comparisons could repeat one destination four times.** Selections
  are now trimmed, deduplicated and checked against the catalogue before the
  four-destination limit. Stored shortlists use the same validation; their
  comparison button counts only the destinations it can actually load.

No climate observations, scoring rules, eligibility decisions, approval flags,
dependencies or hosting architecture changed.

## Verification

- 160 tests pass, including five new comparison/navigation regressions.
- TypeScript, production build and German static-document localization pass.
- Architecture, CSS, representative-cell and deployment-budget guards pass.
- All 370 public data files reproduce byte-for-byte; data validation passes.
- Browser checks on the rebuilt static export: navigation at 390px, 850px and
  1280px; all seven tablet-menu links visible; no horizontal page overflow.
- Browser shortlist regression: save one, enter shortlist, remove it, leave
  using the retained switch. Repeat using Reset filters. Both restore results.
- Browser comparison regression: four copies of Madeira in the URL become one
  column, the URL is normalized, and the search field remains enabled.
- No browser warnings/errors during these checks.

## Still open, not silently approved

- Data quality reports a 13.3°C September-to-October temperature change for
  Hunza. This is a review signal, not proof of bad data; investigate the source
  aggregation before changing any value.
- Four existing persistent-snow holds remain: Denali, El Chaltén, Garhwal and
  Zermatt. Their representative-cell replacements, plus Annapurna's, are
  already tracked in `docs/next-expansion-batch.md` and need fresh source data.
- The release report still lists seven production blockers: source semantics
  and six outstanding approvals (geometry/elevation, licensing, legal/operator,
  accessibility/performance, science audit and custom domain). Passing software
  checks does not grant these approvals.
- The registry security-audit request failed to fetch; this review does not
  claim a completed dependency vulnerability audit.

Routine UI follow-up is Luna-sized. Source interpretation and representative-cell
validation need Sol-level review; legal and other owner approvals need the
appropriate responsible reviewer, not merely a model change.
